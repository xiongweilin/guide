# 精确语义

AI 系统一旦能读取真实数据、代表组织做决定、调用外部服务并产生副作用，很多错误不再来自模型“想得不够聪明”，而是来自软件把本来不同的概念压成了同一个字段或状态。

目标不是创造新术语，而是用已有的软件、分布式系统、安全和治理概念，把这些边界做成可检查的 contract。

## 1. 先明确 source of truth

不同事实应有明确的 authoritative source。

例如：身份目录拥有当前身份状态；HR 系统拥有在职状态；财务系统拥有付款和账务状态；权限系统拥有资源级授权；工作流数据库拥有当前流程状态。

本地缓存、AI 提取、用户输入、同步副本和搜索索引都可以有用，但不能因为“系统已经记录了”就自动成为权威事实。

工程上应显式保存 source、version、timestamp 和 provenance。

## 2. Claim、evidence 和 authoritative data 要分开

用户提交的信息、模型抽取结果、人工证明和 authoritative system readback 具有不同可信度和错误模式。

不要用一个 `value` 字段隐藏来源差异。至少要知道：谁提供了这条信息、它是原始材料还是解释结果、当前是否经过验证、哪个系统有权定义该事实、什么时候需要重新读取权威来源。

AI 输出默认应当是 candidate、draft、recommendation 或 extracted claim，而不是 authoritative fact。

## 3. Identity、role 和 authorization 要分开

Authentication 回答“你是谁”；authorization 回答“你现在能对哪个资源做什么”。

角色、群组、职位、历史授权和当前 permission 也不能自动合并。

常见做法包括：authorization 绑定 operation 和 resource；限定 organization / tenant / project scope；设置有效期；delegation 有明确来源和范围；权限变化后使旧 decision 或 session 失效；高权限操作执行前重新检查 current authorization。

## 4. Policy definition 和 policy evaluation 要分开

一份 policy 是规则定义；某个请求是否满足 policy，是在具体事实和版本上运行规则后的结果。

因此应记录 policy version、evaluation input、evaluation result、依赖的事实版本，以及执行时这些依赖是否仍然 current。

不要只存一个 `approved=true`，否则之后无法解释它为什么成立、依据是否已经变化。

## 5. Decision 和 execution permission 要分开

业务上决定“应该做”不等于运行时已经允许某个进程调用某个 capability。

一个安全的系统通常至少有两层：

1. 业务或组织层确认这项工作应该发生；
2. runtime 根据当前 principal、scope、resource、operation 和 policy 决定这一次调用是否被允许。

这可以避免业务逻辑直接生成底层资源权限。

## 6. 使用显式 state machine，而不是隐式状态组合

长期工作流应把关键状态和 transition 写清楚。

每个 transition 至少应考虑 current state、preconditions、expected version、authorization、operation、resulting state、side effects 和 audit record。

仅仅检查“目标状态是否合法”还不够。一个合法端点也可能通过非法路径到达。

## 7. 用 optimistic concurrency 或等价机制处理并发

长流程中，审批之后事实可能已经变化。

因此执行前应检查 decision 所依赖的 case version、policy version、authority version 或相关 resource version 是否仍然匹配。

常见实现包括 version column、ETag / If-Match、compare-and-swap、optimistic locking 和 immutable decision input snapshot。

旧 decision 不应只因为 ID 还存在就继续有效。

## 8. Freshness 不是“数据刚刚写入”

数据是否可用于当前决策，不只取决于时间戳。

还要考虑 source 是否改变、policy 是否升级、authority 是否撤销、scope 是否扩大、dependency 是否变化、是否存在 unresolved review，以及外部环境是否已经不同。

因此 freshness 更接近“当前依赖仍然有效”，而不是简单的 age check。

## 9. Idempotency 和 effect idempotence 不是一回事

接口接受相同 idempotency key，可以避免重复处理同一请求，但这不代表外部现实副作用天然可重复。

例如付款、发信、创建账号、发放权限等操作，在 provider 超时后仍可能已经成功。

因此需要 request identity、idempotency key、external subject/business key、provider-side lookup、reconciliation 和明确的 retry policy。

结果未知时，默认不应把“再试一次”当作安全动作。

## 10. Transaction 和 outbox 解决不同一致性问题

数据库 transaction 可以保证本地状态原子提交，但无法自动保证数据库和外部消息系统、provider、第三方 API 同时原子完成。

常见模式包括 transactional outbox、inbox / deduplication、saga、compensation、durable workflow engine 和 reconciliation worker。

选择哪种实现取决于业务语义，但都必须能回答：本地状态和外部副作用出现部分成功时，系统怎样恢复到可解释状态。

## 11. Audit log 应保存“发生了什么”，而不是改写历史

重要系统需要 append-only 或等价的不可否认历史。

至少应能追溯 request、input facts、source/version、policy evaluation、decision、authorization、execution attempt、provider response、verification、correction / compensation 和 final outcome。

撤销一个决定不应删除旧决定；修复一个错误也不应让历史看起来“从未发生”。

## 12. Provenance 要连接决策和现实结果

如果系统声称“这次操作产生了这个结果”，就需要能够把链条连接起来：

request → evidence → decision → authorization → execution → external observation → verified outcome。

只看到最终状态符合预期，不能证明它是这次操作造成的。别人可能同时修改了资源，provider 可能重复执行，人工也可能介入。

## 13. Verification 应读取现实，而不是重复读取执行器输出

高影响操作应尽可能使用独立 readback。

例如 writer API 创建账户后，由 reader API 查询最终账户状态；deployment 返回成功后，从健康检查和实际流量验证服务；财务任务完成后，从 authoritative ledger 验证目标记录；权限修改后，从真实 IAM state 检查最终权限。

HTTP 2xx、job completed、模型“确认成功”都不是充分验证。

## 14. Reconciliation 是一等路径

系统应显式支持 ambiguous outcome，而不是强迫所有操作立即归类为 success / failure。

常见状态包括 confirmed success、confirmed failure、pending、unavailable、outcome unknown 和 manual review required。

当 outcome unknown 时，系统的工作是恢复事实，而不是猜测。

## 15. Fail closed 适用于会扩大权限或产生副作用的路径

如果关键 authorization、source、policy version、resource identity 或 provenance 缺失，涉及现实副作用的正向路径通常应拒绝继续。

但 fail closed 不应该伪造事实。

“当前不能授权”不等于“请求一定非法”；“当前无法验证”不等于“操作一定失败”；“source unavailable”也不等于“对象不存在”。

系统需要能够表示 unknown、unavailable、stale、contested 等普通状态，而不是用相反事实填空。

## 16. Revalidation 处理长期有效性

事实、policy、role、模型、依赖和环境发生重要变化后，应重新检查旧 decision 是否仍然可用。

revalidation 的结果可以是继续有效、缩小 scope、需要新 decision、暂停执行、进入人工 review，或者标记旧数据不再适合当前用途。

revalidation 不是自动续期。

## 17. Compatibility 不只是“旧 JSON 还能解析”

真正的兼容还包括语义兼容。

升级时应检查 identity 是否改变、字段含义是否改变、permission 是否扩大、默认值是否改变行为、历史 record 能否仍被正确解释，以及新版本是否会把过去需要 review 的状态自动当作 approved。

解析成功但语义被悄悄改写，是最危险的兼容性问题之一。

## 18. 最小工程检查表

一个会产生真实副作用的 AI / workflow 系统，至少应回答：

1. authoritative source 在哪里？
2. AI 输出被记录成什么类型？
3. authentication 和 authorization 是否分开？
4. policy 是否版本化？
5. decision 绑定了哪些输入版本？
6. 每个 state transition 的 precondition 是什么？
7. provider outcome unknown 时怎么办？
8. 谁负责 independent verification？
9. audit trail 能否重建完整过程？
10. 权限、policy 或 source 变化后怎样 revalidate？
11. rollback 不可用时怎样 compensation？
12. 系统能否安全停止、接管和恢复？

这些都是现有软件工程、安全工程和分布式系统中的普通问题。关键不在创造新名词，而在于不要因为 AI 很强就省略这些边界。
