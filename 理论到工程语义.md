# 理论到工程语义

这篇文档把 [从差异到资格](./从差异到资格.md) 的理论结构翻译成工程语义。

核心原则：**上一个截面已经成立，不代表下一个截面自动取得资格。** 理论可以用“资格”概括“为什么现在足以继续”，但工程实现不能只保存一个 `qualified = true`，而要把不同截面的成立条件拆成不同对象、状态、版本、权限和验证路径。

## 1. 从现实到事实

理论上，现实差异不等于系统已经正确取得和表示了它。

工程上应区分：

`external reality != received data != representation != claim != authoritative fact`

因此要保存 source、version、observed_at、provenance，以及谁有权定义该事实。模型抽取、用户输入和同步副本可以有用，但不能因为已经进入系统就自动升级为 authoritative fact。

## 2. 从候选到决定

理论上，候选解释可以进入当前判断，但当前判断只是“在当前用途下足够采用”。

工程上应区分：

`model output != recommendation != business decision`

Decision 应绑定输入事实、policy version、scope、purpose 和 accountable principal。模型置信度、多模型一致或表达很肯定，都不能直接产生组织 Decision。

## 3. Decision 不等于 Authorization

“应该做”与“现在有权改变现实”是不同截面。

工程上至少要分开：

`Decision != ApprovalSatisfaction != BusinessGrant != RuntimeAuthorization`

授权需要绑定 current principal、operation、resource、scope、authority source 和有效期。业务批准不能自己生成底层资源权限。

## 4. 状态成立不等于迁移成立

合法端点不能证明合法路径。

每个重要 transition 都应检查：

`current state + expected version + preconditions + authorization + input binding -> next state`

因此需要显式 state machine、version binding、optimistic concurrency、immutable decision input 和 audit trail。旧 Decision 不能因为 ID 还存在就永久有效。

## 5. 资格依赖必须可重建、可重验

一个 approval 或 grant 通常依赖：

- 当前事实和 source/version；
- policy version；
- organization / tenant / project scope；
- principal 当前 role / delegation；
- 其他 qualification assessment；
- 未解决 review 是否存在。

工程上应把这些依赖绑定成可追溯 basis，而不是只存 `approved=true`。这样才能回答：“它当时为什么成立？现在还成立吗？”

Freshness 也不只是时间新。source、policy、authority、scope、dependency 或环境改变，都可能使旧资格 stale。

## 6. 执行成功不等于现实效果

行动之后仍然需要新的资格。

工程上要区分：

`dispatch != provider receipt != realized effect != verified outcome`

provider timeout 不能自动解释为“没有发生”，HTTP success 也不能自动解释为“目标已经完成”。真实副作用需要 stable request identity、idempotency / deduplication、ambiguous outcome、reconciliation、authoritative observation 和 semantic verification。

Verification 应读取现实，而不是让 executor 用自己的返回值证明自己成功。

## 7. Outcome 不等于 Completion

某个 effect 已经 verified，不代表整个业务目标完成。

业务 obligation 应独立于 planner 生成：

`facts + policy + governance -> obligation set`

然后再检查：

`obligation set -> permitted effects -> verified outcomes -> completion assessment`

否则 planner 漏掉必要动作时，系统会得到“自洽但错误”的成功。

同样，case completion 也不自动等于长期 responsibility discharge。长期系统还要处理 residual obligation、handoff、successor 和 discharge conditions。

## 8. Review、Revalidation、Reopen、Reauthorization 要分开

条件变化首先意味着“需要重新检查”，不一定自动撤销，也不产生新资格。

- review obligation：出现需要检查的变化；
- revalidation：旧 decision / fact / grant 是否仍适用；
- reopen：恢复事实收集、候选或治理流程；
- reauthorization：重新取得执行或长期权限。

历史记录应保留；当前资格失效不等于历史被删除。

## 9. 责任归属与资格判断正交

工程角色也不能互相冒充：

- parser 生成 representation，不宣布事实为真；
- policy engine 做 evaluation，不给自己资源权限；
- approver 作业务 Decision，不绕过 runtime authorization；
- executor 改变现实，不宣布自己已被独立验证；
- verifier 观察现实，不改写历史；
- recovery / reconciliation 处理不确定结果，不扩大原 scope。

这就是理论中“资格结构”和“责任分配”分开的工程版本。

## 10. 一张反短路表

| 上一步 | 不能自动推出 | 需要新增条件 |
| --- | --- | --- |
| source authenticity | content truth | authoritative source / validation |
| representation | fact | evidence / authority / provenance |
| model confidence | Decision | policy / review / accountable principal |
| Decision | approval complete | required roles / conditions |
| approval | runtime permission | current authorization / scope / resource |
| invocation success | realized effect | reconciliation / readback |
| realized effect | desired outcome | semantic verification |
| one outcome | case complete | independently derived obligations |
| case complete | responsibility discharged | residual obligations / handoff |
| historical validity | current validity | revalidation / reauthorization |

如果一个系统用一个字段跨过表中两行以上，就应检查是否发生语义压平。

## 11. 如何理解 `administrative-orchestrator`

`administrative-orchestrator` 可以看作这些原则在行政数字自动化领域的一种具体化。

它的主链可以这样读：

`Request -> Case -> claims / authoritative facts -> PolicyEvaluation -> current role/delegation qualification -> Decision -> ApprovalSatisfaction -> GovernanceBasis -> AdministrativeObligationSet -> AdministrativeExecutionGrant / EffectIntent -> Kernel Work -> runtime authorization -> RealityBoundary -> external reality -> independent verification -> ConfirmedOutcome -> CompletionAssessment -> complete / reconcile / reassess / reopen`

这条链长，不是为了增加名词，而是因为每一段回答不同问题：

- claim / authoritative fact：这条信息现在以什么地位存在？
- PolicyEvaluation：当前事实是否满足这版规则？
- role / delegation qualification：这个 principal 现在是否仍具备该角色依据？
- Decision：谁在什么输入和 policy 下作出了什么判断？
- ApprovalSatisfaction：必要治理条件是否全部满足？
- GovernanceBasis：这次治理资格具体依赖哪些事实、policy、scope 和 authority？
- ObligationSet：业务上真正必须完成什么？
- AdministrativeExecutionGrant：行政域允许哪些 bounded effect？
- Kernel runtime authorization：这一次 capability invocation 现在允许吗？
- RealityBoundary：真正改变外部现实的边界在哪里？
- verification / ConfirmedOutcome：现实是否满足冻结的 postcondition？
- CompletionAssessment：全部必要 obligation 是否正确满足？
- reassess / reopen / discharge：条件变化后继续、重开还是结束责任？

具体类名和当前行为以 `administrative-orchestrator` 仓库本身为准；guide 只解释这些区别为什么必要，不成为 runtime authority。

## 12. 从理论到测试

理论完成工程翻译后，应能产生明确反例测试，例如：

- AI 高置信输出不能直接成为 authoritative fact；
- stale Decision 不能在 facts / policy / authority 变化后继续使用；
- effect 不能超出 authorization scope；
- provider timeout 不能自动变成 safe retry；
- executor success 不能替代 authoritative readback；
- planner 漏掉 obligation 时不能宣布 completion；
- expected self-induced change 不应错误地使自己的 governance basis stale；
- 真正 dependency 变化必须触发 revalidation / reassessment；
- correction 不能删除原始历史；
- completed case 不能自动表示所有长期责任 discharge。

最小工程骨架可以记成：

`source -> claim -> decision -> authorization -> execution -> observation -> verification -> obligation completion -> revalidation / discharge`

但每个箭头都必须能回答：依据是什么、scope 是什么、谁负责、当前版本是什么、什么允许 transition、什么会使它 stale、如何验证、何时 review / reopen / reauthorize / exit。
