# 从差异到资格

现实中有差异、保持和变化。没有主体时，它们按因果关系、边界条件和自然约束发生，不需要先证明“有资格发生”。

一旦出现会观察、判断、选择、承诺和行动的主体，就多出一个问题：**为什么当前这个观察、解释、判断、规则、行动或状态足以继续成立或进入下一步？**

这里把这个共同问题称为**资格问题**。

资格不是一个统一分数，也不是自动授予的权利。不同领域必须用自己的证据、前置条件、权限、验证和重新评估规则回答它。

## 1. 差异、保持与区分

现实中的差异，不等于系统已经取得并正确表示了差异。

至少要分开：

`现实差异 → 信息取得 → 区分 → 因素 → 关系与组织 → 结构 → 当前方向或判断`

每一层都可能失败：

- 现实有差异，但系统没有取得；
- 取得了信号，但没有正确区分；
- 把噪声当成稳定差异；
- 分类方式错误；
- 漏掉变量、参与者、时间尺度或依赖；
- 结构在当前样本成立，却不能外推。

“保持”也要分开：现实关系持续存在，不等于系统应继续保留旧表示、旧规则或旧权限。

## 2. 从区分到结构需要候选和判断

单个差异不会自动给出正确结构。

系统需要生成候选，比较竞争解释，寻找反例，检查边界和残差，再在当前用途下形成暂时判断。

未知至少有两种：

1. 模型已经定义，只是不知道某个值；
2. 候选空间本身不完整，正确的变量、边界、分类、机制、尺度或参与者还没出现。

因此：

- 没生成候选，不等于候选不可能；
- 当前没找到替代方案，不等于不存在替代方案；
- 当前模型失配，可以先证明“旧结构不够”，而不必同时知道新结构是什么。

残差、反例、跨场景失配和结构张力都可以推动候选空间重新打开。

## 3. AI 可以自动化大量认知步骤

取得差异、压缩表示、生成候选、比较结构、搜索方案、建立模型、发现残差、规划和在给定标准下判断，都可以被高度自动化。

自动化这些步骤，不会自动解决：

- 哪个候选足以被采用；
- 哪个判断足以关闭当前搜索；
- 哪个目标值得承诺；
- 谁有权决定；
- 哪个行动现在允许执行；
- 什么结果足以宣布完成；
- 旧决定现在是否仍然有效。

这些都是资格问题。

## 4. 资格结构

资格结构回答：

> 在当前现实、范围、目的、时间尺度和条件下，一个候选、表示、判断、规则、行动、状态、承诺或关系，凭什么可以保持当前地位、进入下一状态、收缩或扩大自由度、被采用、执行、关闭、继续、退出或重开？

一个具体资格截面通常至少包含：

- 对象、候选或当前状态；
- scope、purpose、timescale；
- 当前开放的自由度；
- 必须保持的约束、接口、身份和关系；
- 支持它的 evidence、proof、policy、authority、commitment 或 verification；
- 谁定义、提出、判断、授权、执行、验证、停止、修复和记录；
- 允许的 transition；
- review、revalidation、exit、retire、reauthorization 或 reopen 触发条件。

不同领域的资格条件不同，不存在一个可以替代它们的通用阈值。

## 5. 最重要的规则：资格不能静默继承

一个截面的成立条件不能自动成为另一个截面的成立条件。

`观察到差异 ≠ 可靠解释`

`可靠解释 ≠ 当前判断应被采用`

`判断可采用 ≠ 目标值得承诺`

`目标值得承诺 ≠ 某人有权决定`

`有权决定 ≠ 运行时已有资源授权`

`请求成功 ≠ 现实效果发生`

`现实效果发生 ≠ 目标完成`

`目标完成 ≠ 长期继续有效`

很多严重错误不是某一步完全错误，而是把上一步的资格直接升级成下一步的资格。

## 6. 状态成立不等于迁移成立

一个结果状态可以合理，但到达它的路径仍可能不成立。

因此必须同时检查：

- 当前状态是否可接受；
- 从旧状态到新状态的 transition 是否有依据；
- transition 是否由正确主体发起；
- 前置条件和当前版本是否仍成立；
- 是否只改变了允许改变的范围；
- 是否留下可追溯的依据。

合法终点不能证明合法路径。

## 7. 收缩、闭合与重开

有限系统不能永久保持所有可能性开放，也不能把临时结论永久化。

常见循环是：

`开放 → 收敛 → 暂时闭合 → 固化经验 → 条件变化后重开`

排除候选、冻结接口、停止搜索、采用标准、作出承诺、限制权限，都是对自由度的收缩，都需要与影响相称的依据。

闭合只表示当前用途下足够继续，不表示永久真理。

当 evidence、scope、environment、dependency、authority、identity、cost、risk 或 purpose 发生重要变化时，旧资格需要 review 或 revalidation。

重开只是恢复候选和选择空间，不自动产生新答案、新决定或新权限。

## 8. 有限性治理

好的系统不是最大化自由，也不是最大化控制，而是在不同状态和时间尺度下配置自由度并保持关键关系。

可以区分：

- 快变量：一次请求、实验、局部动作；
- 中变量：工作流、策略、团队惯例；
- 慢变量：身份、核心接口、制度边界、长期权限。

快变量需要试错，慢变量需要稳定，但慢变量也必须有修改程序。

主动限制搜索、暴露、控制、承诺或开放范围是合理的，只要同时保留理由、期限、退出条件和重开条件。

## 9. 保护未来改变方向的能力

系统不仅要能正常运行，还要保留以后改变方向的能力：

- 还能生成新候选；
- 异议能进入流程；
- 有替代路径和替代依赖；
- 可以退出、恢复、接管；
- 关键权限能重新授权；
- 慢变量可以在明确程序下修改。

表面稳定但失去这些能力，就是 lock-in。

## 10. 资格与责任是两个维度

资格回答“当前是否足以继续”。

责任回答“谁定义、提出、判断、授权、执行、验证、否决、修复、记录和重开”。

责任人存在，不证明判断成立；判断成立，也不自动产生执行权限。

## 11. 多主体后的资格冲突

多个主体可能拥有不同信息、模型、目标、时间尺度、权力来源和承诺。

冲突因此会表现为：

- 谁的证据进入当前判断；
- 谁定义 scope；
- 谁能要求 closure 或 reopen；
- 哪个 authority 被承认；
- 旧承诺是否仍有效；
- 哪些损失和第三方必须进入约束；
- 哪些身份和关系应被保持。

必须区分：

`能力 ≠ 正式权限 ≠ 社会承认 ≠ 正当性`

## 12. 结构迁移的边界

跨领域借用结构时至少区分三种强度：

1. 问题相似：只能借用提问方式；
2. 形式相似：可以借用表示或计算结构；
3. 机制相似：因果机制和边界条件也足够接近，才可借用预测或干预经验。

形式相似不能自动升级为机制证据。

## 13. 自动化后的中心问题

当差异取得、候选生成、结构发现、规划、常规判断和现实执行都越来越自动化，资格也会部分自动化：系统可以自动检查证据、policy、scope、authority、freshness、precondition、verification 和 revalidation。

但一旦不同主体对规则、权限、价值、身份、承诺或正当性本身存在冲突，问题会继续上移：**谁有资格决定什么算有资格。**

这不是执行能力问题，而是治理、制度和价值问题。
