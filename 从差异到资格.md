# 从差异到资格

这篇文档给出整组 guide 的理论主脊柱。它不是要说明宇宙的终极本体，而是解释一个更有限的问题：**一个有限主体如何从现实中的差异与保持出发，形成区分、结构、判断和行动；以及为什么一旦开始有目的地选择，系统就会反复遇到“凭什么可以进入下一步”的问题。**

这个“凭什么可以继续成立、进入下一步、被采用、被执行、被保持或被重新打开”的共同问题，下面简称为**资格问题**。

“资格”在这里不是一个万能分数，也不是一项自动授予的权利。进入具体领域后，它必须拆成该领域自己的证据条件、前置条件、权限、审查、验证和重新评估规则。

## 1. 自然中的差异、保持与变化

在没有主体参与的情况下，现实仍然会发生变化，也会保持某些关系。

星体运行、化学反应、热传导、物种演化、材料形变，都不需要先回答“这个变化有没有资格发生”。它们受现实中的因果关系、边界条件、约束和规律影响，但不需要一个治理程序批准自己。

因此可以先区分两类问题：

- **自然问题**：现实中有什么差异，哪些关系保持，什么会导致什么变化；
- **资格问题**：某个主体为什么可以把一个观察当成证据、把一个解释当成当前判断、把一个决定当成行动依据，或者让一个旧状态继续有效。

前者可以在没有主体的世界中存在；后者只有在出现判断、选择、承诺、行动和责任关系后才出现。

这里的“差异”和“保持”只是工作语言，不宣称它们是世界的最终组成。

## 2. 现实差异不等于系统已经作出区分

现实中存在差异，不代表一个有限系统已经取得、识别并正确表达了这个差异。

一个观察者需要经过取得信息、形成区分、组织表示等步骤，才能把现实中的某些差异变成当前可调用的认知结构。

因此至少要分开：

- 现实中的差异；
- 系统是否取得了相关信息；
- 系统如何区分；
- 区分被表示成什么；
- 这种表示在当前用途下是否足够可靠。

遗漏一个差异、没有取得一个信号、把噪声误当成稳定差异、使用错误分类，都属于不同错误。

## 3. 从区分到结构不是自动推导

一个有限系统通常不会从单个差异直接得到成熟结构。

更接近实际的过程是：

`现实差异 → 取得 → 区分 → 因素 → 关系与组织 → 结构 → 当前方向或判断`

这里每一层都可能存在多个候选。

例如发现两个结果不同，并不能自动告诉我们：

- 哪些变量真正重要；
- 应该按什么方式分类；
- 哪些关系只是相关；
- 哪些关系具有因果作用；
- 当前结构适用于什么尺度；
- 是否漏掉了新的变量或参与者。

所以从区分走向结构，需要 candidate generation、比较、反例、模型批评和判断，而不是只需要计算能力。

## 4. 有限认识不仅是“不知道参数”

未知至少有两种。

第一种是：模型已经定义好，只是不知道某个值。

第二种更困难：当前候选空间本身不完整。可能还没有想到正确变量、边界、分类、机制、时间尺度或参与者。

因此：

- 没生成一个候选，不等于它不可能；
- 当前搜索没找到替代方案，不等于替代方案不存在；
- 一个模型在已有测试中稳定，不等于它覆盖了所有重要现实。

有限系统有时只能判断“当前结构有问题”，却还不知道新结构是什么。残差、反例、跨场景失配和结构张力都可能先告诉我们旧模型不够，再推动新的候选生成。

## 5. AI 可以高度自动化候选、结构和判断过程

现代 AI 已经能大量自动化过去看起来需要高水平洞察的步骤，包括：

- 从复杂输入中取得和压缩差异；
- 生成候选解释；
- 发现形式和结构相似性；
- 搜索方案空间；
- 建立模型和反事实；
- 比较证据；
- 形成计划；
- 发现不一致和残差；
- 在给定标准下作出判断。

这说明“洞察”并不必然是不可自动化的神秘过程。只要有足够表示能力、搜索能力、学习能力和反馈，很多认知步骤都可以被机器承担。

但这并没有自动解决下一类问题：**为什么当前这个候选、判断或计划现在足以被采用，并进一步进入现实行动。**

## 6. 一旦想得到结果，就出现资格问题

只观察世界时，可以不断描述差异与保持。

一旦一个主体想得到某个结果，就必须做选择：

- 哪个观察足以进入当前模型；
- 哪个候选值得继续；
- 哪个判断足以暂时采用；
- 哪个目标值得承诺；
- 哪个主体有权决定；
- 哪个行动现在允许执行；
- 哪个结果足以宣布完成；
- 哪些旧决定还能继续有效。

每一次从“可能”缩小到“采用”，从“知道”进入“做”，从“历史上成立”进入“现在仍然成立”，都在减少或重新配置可选空间。

人一直在隐式回答这些问题，只是日常生活通常不会把它们统一显化。

例如：

- “证据已经够了，所以我现在相信这个解释。”
- “规则已经满足，所以这个请求可以进入下一阶段。”
- “他有这个职位，所以这次决定由他批准。”
- “已经验证现实结果，所以任务可以关闭。”
- “环境已经变了，所以旧承诺需要重新检查。”

这些判断背后都在回答某种“为什么现在足以继续”的问题。

## 7. 资格结构：最高层共同问题

资格问题可以抽象成一个共同结构：

> 在当前现实、范围、目的、时间尺度和条件下，一个候选、表示、判断、规则、行动、状态、承诺或关系，凭什么可以保持当前地位、进入下一状态、收缩或扩大自由度、被采用、被执行、被关闭、继续存在、退出或重新打开？

这个共同结构下面简称为**资格结构**。

一个具体资格判断通常至少依赖：

- 当前对象、候选或状态；
- scope、purpose 和 timescale；
- 当前仍然开放的自由度；
- 必须保持的约束、接口、身份和关系；
- 支持它的 evidence、proof、policy、authority、commitment 或 verification；
- 谁定义、提出、判断、授权、执行、验证、停止、修复和重新评估；
- 当前允许的 transition；
- 哪些变化触发 review、revalidation、exit 或 reopen。

这不是一个统一公式。不同领域有不同成立条件。

## 8. 最重要的规则：资格不能静默跨层继承

一个截面的成立条件，不能被偷偷当成另一个截面的成立条件。

例如：

`观察到差异 ≠ 已经形成可靠解释`

`解释足够可靠 ≠ 可以停止所有搜索`

`当前判断成立 ≠ 这个目标值得追求`

`目标值得追求 ≠ 某个主体有权决定`

`有权决定 ≠ 某个程序已经获得资源级授权`

`执行请求成功 ≠ 现实效果已经发生`

`现实效果发生 ≠ 目标已经达成`

`目标达成 ≠ 长期继续有效`

这条规则是理论和工程之间最重要的桥。

很多严重错误并不是某一步完全错误，而是**把上一步的资格自动升级成下一步的资格**。

## 9. 状态成立和迁移成立也要分开

一个结果状态本身合理，不代表到达它的路径合理。

例如某个账号最终确实存在，不代表：

- 创建它的人有权限；
- 使用的是当前 policy；
- 中间没有重复创建；
- 这次操作就是产生结果的原因；
- 没有同时扩大不该扩大的权限。

因此要区分：

- 当前状态是否可接受；
- 从旧状态迁移到新状态的路径是否有资格。

在工程系统中，这会直接变成 precondition、version binding、authorization、state transition、provenance 和 verification 问题。

## 10. 收缩、闭合和重开

有限系统不能永远保持所有候选开放，也不能过早永久关闭。

常见循环是：

`开放 → 收敛 → 暂时闭合 → 固化可复用经验 → 条件变化后重开`

任何排除候选、冻结接口、停止搜索、采用标准、作出承诺或限制权限，都是对自由度的收缩，都应该有与影响相称的依据。

闭合只表示“当前用途下足够继续”，不表示永久真理。

当 evidence、scope、environment、dependency、authority、identity、cost、risk 或目的发生重要变化时，旧资格可能需要重新评估。

重开也不自动产生新的答案或新的执行权限，它只是恢复一部分候选和选择空间。

## 11. 资格和责任归属是两个维度

“这件事现在是否成立”和“谁负责它”不能混成一个问题。

资格回答：

- 当前条件是否足够；
- 是否可以继续；
- 是否需要缩小、停止或重开。

责任归属回答：

- 谁定义条件；
- 谁提出候选；
- 谁判断；
- 谁授权；
- 谁执行；
- 谁验证；
- 谁能否决；
- 谁负责修复；
- 谁负责记录和重新评估。

一个人承担责任，并不证明某个判断成立；一个判断成立，也不自动告诉我们谁有权把它变成现实。

## 12. 多主体后，资格会发生冲突

多个主体不一定共享完整事实、模型、目标、权力来源或时间尺度。

因此冲突不只是“大家对同一事实意见不同”，还可能是：

- 谁的证据算数；
- 谁能定义 scope；
- 哪个 authority 被承认；
- 一个旧承诺是否仍然有效；
- 谁能要求 closure；
- 谁能要求 reopen；
- 哪些损失必须进入约束；
- 哪些身份和关系应该继续被保持。

这里还必须区分：

`能力 ≠ 正式权限 ≠ 社会承认 ≠ 正当性`

一个主体有能力做到，不代表它有权做；有权做，不代表别人承认；被承认，也不自动证明制度本身正当。

## 13. 价值不能被压成一个层次

当讨论“什么结果值得追求”时，至少要区分：

1. 系统层面的功能差异和调节方向；
2. 是否存在主观体验以及体验的正负性质；
3. 个体经过反思后认同的价值与承诺；
4. 涉及他人、权利、公共事务和制度正当性的规范判断。

从 reward、homeostasis、目标追踪、自我维护、复杂行为、语言或自我模型，都不能自动推出主观体验，更不能直接推出公共规范结论。

每一次跨层都需要新的证据和判断条件。

## 14. 高度自动化后，资格密度会上升

AI 可以继续自动化认知，机器人可以继续自动化现实执行。

常规资格检查本身也可以大量自动化，例如：

- evidence completeness；
- policy evaluation；
- role / scope 检查；
- freshness；
- transition preconditions；
- verification；
- revalidation；
- reconciliation。

这会释放大量原本由人承担的协调、审批和执行工作。

但自动化并不会消除更高一层的问题：

- 谁有资格制定这些 qualification rule；
- 这些 rule 为什么继续有效；
- 谁能修改宪制性规则；
- 不同主体的资格结构冲突时采用什么程序；
- 哪些价值、身份、权利和长期承诺必须被保留。

所以技术越能自动化“怎么做到”，公共冲突越可能集中到“凭什么这样做、由谁决定、有效到什么时候”。

## 15. 工程化不是实现一个 `qualified = true`

资格结构是最高层抽象，不是一个应该直接进入 runtime 的万能对象。

工程系统必须把它拆开。

例如：

- 认识问题使用 evidence、source、confidence、validation、review；
- workflow 使用 admission criteria、preconditions、state transition；
- 治理使用 policy、decision、scope、current authority；
- 资源执行使用 authentication、authorization、capability permission；
- 外部效果使用 idempotency、reconciliation、observation、verification；
- 长期有效性使用 freshness、revalidation、reauthorization；
- 责任使用 owner、approver、executor、verifier、reviewer、recovery role。

如果一个系统把这些重新压成一个布尔字段，它就失去了这套理论最重要的信息。

下一步可以阅读 [理论到工程语义](./理论到工程语义.md)，看这些理论区别怎样落成工程 contract；再阅读 [精确语义](./精确语义.md) 和 [行动、授权与可靠性](./行动、授权与可靠性.md) 看具体实现原则。
