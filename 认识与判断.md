# 认识与判断

人的判断始终发生在有限条件下：时间有限、观察渠道有限、工具有限、已有知识有限，问题本身也可能定义错。AI 同样如此。模型可以更快、更广，但它仍然只能基于取得的输入、已有表示和可调用工具工作。

因此，判断的目标不应是“假装已经掌握完整现实”，而应是：**在当前用途下形成足够可靠的工作结论，同时明确它依赖什么、哪里可能错、什么新事实会要求修改。**

## 1. 模型不是现实

至少要区分四件事：

- 现实中发生了什么；
- 我们实际观察到了什么；
- 我们如何解释这些观察；
- 我们最终暂时采用了什么判断。

这四层常被混在一起。

例如，一个接口返回 `success`，只是一个系统输出；“业务目标已经完成”是进一步解释；“可以关闭任务”又是一个决定。中间缺少任何一层，都可能产生错误。

同样，分类、标签、模型字段、数据库记录都只是工作表示。名称相同、字段相同、量纲相同，不自动证明两个对象在不同时间和情境下仍然是同一个东西。

## 2. 未知不只有一种

常见的不确定性有两类。

第一类是**已知问题中的未知**：我们知道变量、对象和问题结构，只是不知道值。

例如：

- 某项成本是多少；
- 某台机器是否在线；
- 某个候选方案的成功概率是多少。

第二类更难：**问题空间本身不完整**。

可能缺少：

- 一个重要变量；
- 一个对象类型；
- 一个关键参与者；
- 一个时间尺度；
- 一个新的解释；
- 一个更合理的分类方式；
- 一种新的取证方式；
- 甚至整个问题定义。

这意味着“当前没想到”不能直接等于“不存在”，“当前搜索不到”也不能直接等于“不可能”。

这对 AI 尤其重要。模型可以在一个既定问题空间中搜索得非常强，但它仍可能遗漏问题空间之外的关键候选。

## 3. 好的候选不是数量多，而是能区分

面对复杂问题，不应只追求更多想法，而应优先寻找**能产生不同可观察后果的候选**。

一个新解释如果只是换一种说法，却不会改变：

- 需要收集什么证据；
- 应该预测什么；
- 哪个反例能否定它；
- 下一步该做什么；

那么它的价值通常很低。

真正有用的候选，会改变后续判断。

## 4. 异常首先告诉你“哪里可能需要重看”

一个反例并不自动说明整套模型都错了。

失败可能来自不同层级：

- 数据没取得；
- 取得了，但分类错了；
- 漏掉了关键变量；
- 变量关系建错了；
- 时间尺度选错了；
- 目标或评价标准有问题；
- 验证方法本身有偏差；
- 问题定义失效。

好的修正不是“每次失败都大改理论”，也不是“每次失败都调一个参数”，而是寻找**最浅、但足以解释失败，并且可以被新证据区分的修正层级**。

修得太浅，会把结构性错误当噪声；修得太深，会把局部故障误判成整个问题失效。

## 5. 继续搜索也有成本

更多信息不总是更好。

继续搜索会消耗：

- 时间；
- 算力；
- 注意力；
- 决策窗口；
- 现实机会；
- 额外风险。

因此需要比较：下一单位搜索是否还能显著改变判断。

当新增信息的区分价值已经很低，而继续拖延的成本开始上升时，停止搜索可能是合理的。

但停止不等于“真理已经确定”。更准确的说法是：

> 在当前目的、证据和时间条件下，这个判断已经足够支持下一步。

同时应保留：

- 主要未知；
- 适用范围；
- 关键假设；
- 哪些新事实会要求重新评估。

## 6. 认知上的闭合不等于行动许可

这是最重要的边界之一。

“我已经足够确定这个解释”不等于：

- 我有权执行；
- 风险可以接受；
- 第三方同意；
- 外部资源已经授权；
- 操作成功后不需要验证。

认知判断解决的是“当前应相信或采用什么”。现实行动还需要另外的权限、程序、风险控制和验证。

AI 系统中尤其不能把模型置信度当成行动授权。

## 7. 经验应该沉淀什么

如果只保存成功答案，系统很容易重复犯同样的结构性错误。

更有价值的是保存：

- 当时的适用条件；
- 哪些候选被否决；
- 哪些信号后来证明没用；
- 出现过哪些反例；
- 为什么修改判断；
- 当时哪些未知仍未解决；
- 什么条件下旧结论应该重新检查。

这样保存的不是“答案”，而是以后更快形成和修正答案的能力。

## 8. 对 AI 系统的直接含义

一个可靠的 AI 系统不应把所有认知状态压成一个 `confidence`。

至少应该能够区分：

- 原始输入；
- 来源；
- 解释；
- 候选；
- 当前采用的判断；
- 仍未解决的冲突；
- 需要补充的观察；
- 是否可以停止当前搜索；
- 何时需要重新评估。

更强的模型可以改善候选生成和分析，但不能自动消除这些边界。

## 9. 一组实用问题

遇到复杂问题时，可以依次问：

1. 我实际看到了什么，而不是我解释出了什么？
2. 哪些变量已经进入模型，哪些关键变量可能根本还没出现？
3. 当前结论依赖哪些假设？
4. 哪个新观察最能区分两个主要解释？
5. 当前异常应该改参数、结构、取证方式，还是问题定义？
6. 继续搜索还能带来多少新的区分价值？
7. 当前结论适用于哪里，不适用于哪里？
8. 什么事实出现后必须重新评估？

核心原则只有一句：

> **允许暂时确定，但不要把暂时确定伪装成永久正确。**
