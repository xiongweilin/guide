# 现实、约束与变化

现实中的系统会变化，也会在变化中保持某些关系。生物会维持体温，软件会保持接口兼容，组织会保持角色和流程，基础设施会保持服务能力。

更实用的问题不是建立一套新的总理论，而是：**什么可以变化，什么必须暂时保持，谁可以修改规则，什么时候应该重新检查这些约束。**

## 1. 自然变化与治理变化

没有主体参与时，变化受物理规律、边界条件和因果关系限制。

一旦出现会判断、选择、承诺和行动的主体，就会多出另一类问题：

- 哪些变化允许发生；
- 哪些状态必须保持；
- 谁可以修改规则；
- 什么证据足以支持下一步；
- 哪些后果必须独立验证；
- 哪些旧决定需要重新评估。

这些不是自然规律本身，而是 decision-making、governance、authorization 和 review 的问题。

## 2. 约束不是越多越好，自由也不是越多越好

稳定接口会限制局部实现，却可能增加系统整体的可组合性；权限边界会限制单个执行者，却可能减少事故传播；长期承诺会关闭一部分选择，却可能让协作成为可能。

因此更有用的问题是：

- 哪些自由度应该开放；
- 哪些应该暂时收紧；
- 哪些关系需要稳定；
- 哪些限制必须有期限；
- 哪些规则必须保留 review 和修改程序。

目标不是最大化自由或最大化控制，而是让局部变化不会摧毁系统整体的适应能力。

## 3. 不同时间尺度需要不同处理

一个系统通常同时包含快变量、中等速度的过程和慢变量。

例如：

- 快：一次请求、一次实验、一次局部调整；
- 中：工作流、习惯、团队惯例、策略；
- 慢：身份、核心接口、制度边界、基础设施选择。

所有东西都快速变化，系统会漂移；所有东西都固定，系统会僵化。

所以需要区分：哪些东西应该快速试错，哪些需要稳定一段时间，哪些虽然稳定但必须保留明确的修改程序。

## 4. 每一次收紧可选空间都需要理由

实践中，很多决定都在减少可选空间：

- 排除一个候选解释；
- 冻结一个接口；
- 采用一个标准；
- 限制某类操作；
- 缩小项目范围；
- 停止继续搜索。

这些动作不应因为“当前最方便”就自动成立。

至少要问：

- 支持这个决定的证据是什么；
- 证据适用于多大范围；
- 是否把“暂时没找到”误写成“不存在”；
- 是否为了适配当前方案偷偷修改了原标准；
- 哪些条件变化后需要重新评估。

## 5. 当前稳定不等于永久有效

一次成功、一段时间稳定、一次验证通过，都不能自动推出长期继续有效。

需要重新检查的常见原因包括：

- 环境变化；
- 依赖变化；
- 新的反例；
- 新的参与者；
- 权限或责任变化；
- 原来的替代方案消失；
- 原来的恢复路径不再可用；
- 长期成本开始超过短期收益。

这也是为什么长期系统需要 review、revalidation 和 reauthorization，而不是一次批准永久有效。

## 6. 保持未来改变方向的能力

系统设计不能只保护当前正常运行，还要保护以后改变方向的能力。

需要关注：

- 是否还能产生新的方案；
- 是否保留异议渠道；
- 是否有替代供应商、替代接口或替代路径；
- 是否可以安全退出；
- 是否有独立恢复能力；
- 是否还能重新取得控制权；
- 修改关键规则需要什么程序。

一个系统可以今天很稳定，却已经失去未来改变方向的能力。这样的稳定往往只是 lock-in。

## 7. 常见的结构性失败

可以用几类简单问题快速定位。

### 漏掉了重要差异

例如漏了参与者、失败模式、时间尺度或依赖关系。

### 把噪声当成稳定差异

例如过度分类、把偶然相关当作固定属性。

### 变化中丢失了本应保持的东西

例如升级破坏兼容、迁移丢失历史来源、组织调整后责任无人承接。

### 已失效的东西被默认继续保留

例如临时权限长期化、过期模型继续使用、旧政策没有复核。

这些只是诊断方法，不是完整分类学。若现实问题无法被它们解释，应修改分类，而不是强迫现实适应分类。

## 8. 跨领域类比要控制强度

一个结构在多个领域看起来相似，不代表它们具有相同机制。

可以区分三种迁移强度：

1. **问题相似**：可以借用提问方式；
2. **形式相似**：可以借用模型或计算结构；
3. **机制相似**：只有因果机制和边界条件也足够接近时，才可以借用预测或干预经验。

从形式相似直接跳到机制相似，是跨领域讨论中最常见的错误之一。

## 9. 一个实用的循环

复杂任务通常不是“一次分析然后永久结束”，而是反复经历：

1. 扩大候选和信息；
2. 用证据逐步排除；
3. 在当前用途下形成暂时结论；
4. 把真正有复用价值的经验记录下来；
5. 环境、证据或目标变化后重新评估。

这个循环既避免无限分析，也避免把临时结论永久化。

## 10. 对高度自动化系统的含义

当 AI 逐渐自动化信息处理、规划和常规决策，机器人进一步自动化现实执行后，系统的难点会越来越集中在：

- 谁能定义规则；
- 哪些条件允许自动执行；
- 哪些决定必须由特定主体参与；
- 哪些权限不能由模型自己生成；
- 哪些长期结构需要重新授权；
- 当不同主体对规则本身有冲突时，采用什么决策程序。

技术可以降低执行成本，但不会自动消除规则冲突、权力冲突和价值冲突。

核心原则是：

> **稳定和变化都需要条件；好的系统既能收敛，也能在必要时改变方向。**
