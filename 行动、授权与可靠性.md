# 行动、授权与可靠性

一个判断即使正确，也不代表可以直接改变现实。现实行动会影响持久状态、共享资源、第三方利益和未来选择，因此需要把“知道什么”“决定什么”“谁有权做”“实际发生了什么”分开。

## 1. 判断、决定、授权、执行和结果是不同事件

实践中至少要区分：

- 判断：当前认为哪个解释或方案更合理；
- 决定：在一个明确程序中选择做什么；
- 授权：某个主体是否被允许执行特定操作；
- 执行：系统是否真的发起并完成了动作；
- 结果：现实是否变成了目标状态；
- 验证：是否有独立证据确认结果和副作用。

AI 可以很好地参与判断和方案生成，但不能因为“建议质量高”就自动获得现实权限。

## 2. 风险越高，程序越强

程序强度应随着以下因素提高：

- 影响范围；
- 不可逆性；
- 第三方影响；
- 恢复难度；
- 权力不对称；
- 错误传播速度；
- 对关键基础设施的依赖。

低影响、容易恢复的操作可以保持简单；高影响操作则需要更严格的授权、独立验证、限流、接管和退出机制。

以下情况通常应直接提高控制强度，而不是等待综合评分：

- 涉及身体安全、隐私、重要权利或重大经济损失；
- 授权来源不清；
- 关键变化不可逆；
- 受影响者缺乏真实退出或申诉；
- 错误可能快速复制或扩散；
- 执行器和验证器共享明显的单点故障；
- 法律、合同或专业规范明确要求更高程序。

## 3. 先限制暴露，再逐步扩大

证据弱、影响大或恢复困难时，应优先限制：

- 对象范围；
- 权限范围；
- 持续时间；
- 并发量；
- 不可逆操作；
- 外部副作用。

小范围成功只能支持当前条件下的下一步。扩大用户、资源、权限、运行时间或不可逆程度，都是新的风险条件，需要重新评估。

## 4. API 成功不等于现实成功

外部系统返回 200、任务队列标记完成、模型声称成功，都只能说明某个局部步骤结束。

真正需要验证的是：

- 目标状态是否已经形成；
- 是否产生未预期副作用；
- 关键假设是否仍然成立；
- 是否影响第三方或共享资源；
- 是否需要补救、回滚或补偿。

重要操作应尽可能用独立观察验证，而不是让执行器自己证明自己成功。

## 5. 结果不确定时，不要盲目重试

分布式系统和外部 API 经常出现一种危险状态：请求超时了，但你不知道副作用是否已经发生。

这时“请求失败”不等于“现实没有变化”。如果直接重试，可能造成重复付款、重复创建、重复通知或重复授权。

正确做法通常是：

1. 暂停新的副作用；
2. 查询外部真实状态；
3. 通过幂等键、业务标识、审计记录或 provider 查询重建发生了什么；
4. 再决定继续、补偿还是人工处理。

这就是 reconciliation：先恢复对现实的认识，再决定下一步。

## 6. 回滚并不等于真正可逆

软件里有 rollback，不代表现实后果可恢复。

需要区分至少三种不可逆性：

- **状态不可逆**：数据、身体、关系、信誉或资源损失无法恢复；
- **控制不可逆**：知道出错了，但已经失去停止、修改、接管或退出权限；
- **认识不可逆**：关键数据、知识、人员或验证能力已经丢失，导致无法再判断发生了什么。

真正的恢复能力，需要同时保留状态恢复、控制入口和判断能力。

## 7. 补偿也是责任的一部分

有些变化无法完全撤销。

此时系统仍可能需要承担：

- 修复；
- 补偿；
- 人工照护；
- 申诉；
- 长期跟踪；
- 向受影响者说明和纠正。

“任务结束”不等于责任结束。

## 8. 长期可靠性不是一个分数

长期系统最重要的不是得到一个“可靠性 92 分”，而是知道它会怎样失败。

至少检查：

### 观察

新的现实差异能否进入系统？

### 异议

异常、反例和受影响者的信息能否进入正式流程？

### 验证

评价机制是否独立到足以发现执行器自己的错误？

### 修正

发现错误后能否真正修改模型、规则、权限和现实状态？

### 记忆

旧版本、事故、反例和修订理由是否可追溯？

## 9. 多个检查者不等于独立

两个模型、两个团队或两个系统，如果共享同一数据源、指标、供应商、身份系统、权限来源或基础设施，仍然可能一起失败。

可靠性取决于失败相关性，而不是“检查者数量”。

所以需要显式分析 common-cause failure 和 fault domain，而不是只增加更多审批节点。

## 10. 正常依赖、故障传播和恢复依赖要分开

系统平时怎么运行、故障怎样传播、故障后靠什么恢复，往往是三张不同的依赖图。

两个服务平时互不依赖，却可能在灾难恢复时都依赖同一个身份系统、密钥库、云账号或维护团队。

因此，运行冗余不自动等于恢复冗余。

## 11. 纠错速度要赶得上损伤速度

一个系统即使“理论上可以发现并纠正错误”，如果发现、判断和停止所需时间已经超过损伤形成时间，这套纠错机制在现实中仍然无效。

应比较：

- 错误传播速度；
- 损伤积累速度；
- 发现延迟；
- 人工判断延迟；
- 自动停止延迟；
- 恢复所需时间。

这比简单讨论“自动化更快还是人工更安全”更有意义。

## 12. 高权限必须定期重新授权

长期存在的高权限、紧急权限和例外不应因为曾经合理就永久保留。

至少应绑定：

- 明确范围；
- 有效期；
- 依据；
- 复核者；
- 到期后的默认状态；
- 异常情况下的接管和终止方式。

临时集中可以合理，但不能悄悄变成永久结构。

## 13. 一个实用的变化流程

重要变化可以按以下顺序检查：

1. 明确对象、目的和完成标准；
2. 确认当前事实和主要未知；
3. 确认谁能决定、谁能执行、谁能验证；
4. 设计最小影响范围和恢复方案；
5. 执行并记录真实输入、版本和副作用；
6. 从现实结果独立验证；
7. 继续、扩大、缩小、补偿、停止或重新评估；
8. 长期运行时定期复核权限、依赖和退出能力。

核心原则是：

> **知道怎么做，不等于有权做；做了，不等于成功；成功一次，不等于应该永久继续。**
