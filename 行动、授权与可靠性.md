# 行动、授权与可靠性

从“知道什么”到“改变现实”之间，至少要分开判断、技术能力、正式授权、执行、现实效果、验证和长期责任。

## 1. 知道怎么做不等于有权做

必须区分：

- 认知上足以提出方案；
- 技术上能够执行；
- 制度上被授权执行；
- 运行时当前权限仍有效。

任何一层成立，都不能自动替代下一层。

## 2. 什么时候需要显式行动程序

当行动会影响以下对象时，应进入显式、可追溯的行动程序：

- 持久状态；
- 正式记录；
- 共享资源；
- 第三方；
- 身份、权利或声誉；
- 长期依赖；
- 难以恢复的未来路径。

风险越高，程序越应加强。安全、隐私、权利、不可逆损害、权限不清、无恢复路径、权力不对称、快速传播、系统锁定、关键基础设施和法律义务，都应触发更严格的审查、批准、验证或人工参与。

## 3. 行动至少有六个截面

1. **现实与目的**：当前事实是什么，要改变什么；
2. **条件与权限**：前置条件、规则、权限依据和作用范围是否成立；
3. **计划与暴露**：怎样做、影响范围多大、何时停止；
4. **执行与记录**：实际发送了什么、发生了哪些副作用；
5. **验证与修正**：现实是否达到预期结果，是否需要核对现实状态、补偿或修正；
6. **持续、退出与重开**：结果能否继续保持，何时重新验证、重新授权、交接、退役或重开。

这些截面不能自动继承资格。

## 4. 限制暴露，再逐步扩大

新方案、新模型和新权限应优先从有限范围、有限资源、有限用户或有限时间窗口开始。

扩大范围前重新检查：

- 事实和假设是否仍成立；
- 失败方式是否变化；
- 恢复路径是否真实可用；
- 权限是否覆盖新范围；
- 验证方式是否仍能区分成功和失败。

局部成功不能自动证明更大规模、更长时间、更高权限或更高不可逆度也成立。

## 5. 外部服务成功不等于现实成功

外部操作要区分：

`请求发出 → 外部服务接收 → 副作用可能发生 → 现实状态可观察 → 目标结果被验证`

因此：

- HTTP 2xx 不等于目标完成；
- 超时不等于副作用没有发生；
- 命令报错不等于现实中一定没有变化；
- 请求去重不等于现实副作用可以安全重复。

结果不明确时，应先隔离进一步影响，读取权威现实状态，核对实际结果，再决定重试、补偿、人工处理或关闭。

## 6. 验证必须来自现实

执行器不能仅用自己的返回值证明自己成功。

高影响行动应尽可能独立读取现实状态，并检查：

- 对象身份；
- 预期结果；
- 作用范围；
- 非预期副作用；
- 版本和当前有效性；
- 结果与本次执行之间的可追溯关系。

最终状态符合预期，也不能单独证明是本次操作造成的。

## 7. 回滚不等于可逆

回滚代码或数据库状态，不代表现实已经恢复。

恢复需要考虑：

- 数据；
- 关系；
- 身份和权限；
- 权利、声誉和机会；
- 资源和成本；
- 已扩散的信息；
- 知识和路径依赖。

即使结果最后可接受，也可能留下后续责任，需要补偿、通知、持续观察或后续修复。

## 8. 最小行动记录

重要变更至少应能重建：

- 对象、范围、目的和完成条件；
- 证据、假设和未知；
- 角色、权限依据和批准；
- 计划、暴露范围和停止条件；
- 执行尝试、副作用和外部服务引用；
- 现实观察、验证和结果；
- 修正、回滚、补偿、交接和退出；
- 继续、扩大、缩小、退役、重新验证、重新授权和重开的条件。

## 9. 长期资格需要重新授权

长期运行会积累：

- 暴露范围；
- 临时权限长期化；
- 维护和审计成本；
- 集中化；
- 依赖；
- 恢复和退出难度；
- 旧证据、旧规则、旧权限与当前现实的偏差。

高影响或长期权限不应一次授权永久有效。应有明确的重新审查、重新验证和重新授权周期或触发器。

## 10. 可靠性是结构，不是总分

可靠性至少包括：

- 能否观察错误；
- 异议能否进入；
- 是否有独立验证；
- 能否停止；
- 能否隔离；
- 能否修正；
- 能否恢复；
- 是否保存历史；
- 能否退出或接管。

多个检查者如果共享同一数据、模型、指标、资金、身份基础设施、技术根或权限来源，就可能因同一个原因同时失效，不能按真正独立的冗余计算。

## 11. 三张依赖网

应分别检查：

1. 正常运行依赖；
2. 故障传播依赖；
3. 恢复依赖。

正常路径冗余，不等于恢复路径冗余。

还要比较速率：检测、判断、停止、恢复和重新授权的速度，是否赶得上损害传播和系统锁定的速度。

## 12. 有效可逆性与机动性

可逆性至少包含：

- 状态可逆：能否恢复对象状态；
- 控制可逆：能否重新取得控制权；
- 认知可逆：还能否知道发生了什么、为什么发生。

机动性还要求：能生成替代方案、存在可达路径、承受转换成本、重建前置条件并重新取得授权。

最严重的系统锁定不是“切换很贵”，而是系统已经失去重新生成选项和恢复控制的能力。
